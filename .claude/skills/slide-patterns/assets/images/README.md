# Image Assets

画像はスキルに同梱しない方針。プロジェクトごとに以下のディレクトリに配置する:

- `assets/images/cover/` カバー背景画像 (S1)
- `assets/images/section/` 章扉背景画像 (S2)
- `assets/images/map/` 世界地図 (WM1)
- `/mnt/user-data/uploads/` ユーザーアップロード画像 (案件別)
